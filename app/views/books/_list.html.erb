<h2>User info</h2><br>
<table class='table'>
  <tr><%= attachment_image_tag user, :profile_image, :fill, 60, 60, class: "pull-left profile-thumb",  fallback: "download.jpg"%></tr>
  <tr>
    <th>name</th>
    <th><%= user.name %></th>
  </tr>
  <tr>
    <th> introduction</th>
    <th><%= user.introduction %></th>
  </tr>
</table>
<div class='row'>
  <%= link_to "edit",edit_user_path(user.id), class:"col-xs-12 btn btn-default glyphicon glyphicon-wrench edit_user" %>
</div>

<h2>New book</h2>
<%= form_for(book_new) do |f| %>

   <% if book_new.errors.any? %>
<%= book_new.errors.count %>errors prohibited this obj from being saved:</h3>
     <% book_new.errors.full_messages.each do |message| %>
       <%= message %>
     <% end %>
     <% end %>

   <h4>Title</h4>
   <%= f.text_field :title,class:'col-xs-12' %>
   <h4>Opinion</h4>
   <%= f.text_area :body,class:'col-xs-12' %><br>
   <%= f.submit 'Create Book',class:"btn btn-primary col-xs-12" %>


   <div class="actions row">
  </div>
<% end %>

